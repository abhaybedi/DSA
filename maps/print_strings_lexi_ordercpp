#include<bits/stdc++.h>
using namespace std;

int main(){
    map<string,int> map;
    int n;
    cin>>n;
    for(int i=0;i<n;i++){
        string s;
        cin>>s;
        map[s]++;
    }
    for(auto it:map){
        cout<<(*it).first<<" "<<(*it).second<<endl;
    }
}